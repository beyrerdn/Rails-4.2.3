<%= render :partial => "post", :locals => {:post => @post} %>

<% if current_user %>
  <div class="form"><%= render :partial => "comments/form", :locals => {:commentable_object => @post} %></div>
<% end %>

<div class="view-comments"><%= render :partial => "comments/comment", :collection => @post.comments %></div>
